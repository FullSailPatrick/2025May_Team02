name: Discord feed

on:
  push:
    branches: [ "**" ]

jobs:
  feed:
    uses: FullSailPatrick/workflow-library/.github/workflows/discord.yml@main
    secrets:
      webhook: ${{ secrets.DISCORD_WEBHOOK }}
