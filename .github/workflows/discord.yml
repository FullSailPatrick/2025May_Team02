- name: Post to Discord
  env:
    WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
  run: |
    echo "white_check_mark: GitHub push detected" > message.txt
    echo "DEBUG length=${#WEBHOOK}"              # <-- temporary
    …
